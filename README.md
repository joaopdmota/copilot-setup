# Copilot OS (GitHub-contained)

This is your Antigravity system converted to a Copilot-native layout.

Everything related to AI behavior lives inside `.github/`.

Use `.github/copilot-instructions.md` as the main entry point.
